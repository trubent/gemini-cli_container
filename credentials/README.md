# Local credentials folder

Put local credentials here only on your machine.

Examples:

- gcp-service-account.json
- terraform.tfvars.local
- kubeconfig
- vault-token

This folder is ignored by Git except this README and .gitkeep.
